substitutions:
  device_name: pet-feeder
  topic_prefix: wifi2mqtt/pet-feeder

esphome:
  name: ${device_name}
  friendly_name: Pet Feeder

esp32:
  board: esp32-c3-devkitm-1
  framework:
    type: esp-idf

logger:

ota:
  - platform: esphome
    password: "e65a575ea5facec072c4c02ee4195dd6"

wifi:
  networks:
    - ssid: !secret wifi_ssid
      password: !secret wifi_password
  fast_connect: true
  power_save_mode: none
  reboot_timeout: 3min
  ap:
    ssid: "Pet-Feeder Fallback"
    password: !secret ap_password

captive_portal:

mqtt:
  broker: !secret mqtt_broker
  port: !secret mqtt_port
  username: !secret mqtt_user
  password: !secret mqtt_password
  client_id: ${device_name}
  topic_prefix: ${topic_prefix}
  discovery: true
  birth_message:
    topic: ${topic_prefix}/availability
    payload: online
    retain: true
  will_message:
    topic: ${topic_prefix}/availability
    payload: offline
    retain: true
  on_connect:
    then:
      # Signal MCU that WiFi module is ready — MCU will start sending heartbeats
      - output.turn_on: mcu_en_pin
      - mqtt.publish:
          topic: ${topic_prefix}/availability
          payload: "online"
          retain: true
      - mqtt.publish:
          topic: ${topic_prefix}/status
          payload: "Idle"
          retain: true
      - script.execute: do_handshake
  on_disconnect:
    then:
      - lambda: "id(mcu_connected) = false;"
  on_message:
    # Feed command from HA: payload = number of portions (1-12)
    - topic: ${topic_prefix}/feedCmd
      then:
        - script.execute:
            id: feed_script
            portions: !lambda "return atoi(x.c_str());"

# ── PIN ASSIGNMENTS (ESP32-C3 Super Mini) ────────────────────────────────────
#  WBR3 pad   → C3 GPIO  Function
#  Pin 8 VCC  → 3V3      Power
#  Pin 9 GND  → GND      Ground
#  Pin16 TXD  → GPIO20   UART RX (ESP receives MCU TX)
#  Pin15 RXD  → GPIO21   UART TX (ESP sends to MCU RX)
#  Pin EN     → GPIO2    WiFi module ready signal (driven high after handshake)
# ─────────────────────────────────────────────────────────────────────────────

uart:
  id: tuya_uart
  tx_pin: GPIO21
  rx_pin: GPIO20
  baud_rate: 9600

output:
  # EN pin: drive high to signal MCU that WiFi module is ready
  - platform: gpio
    id: mcu_en_pin
    pin:
      number: GPIO2
      mode:
        output: true

globals:
  - id: feeding_active
    type: bool
    restore_value: no
    initial_value: 'false'

  - id: mcu_heartbeat_seen
    type: bool
    restore_value: no
    initial_value: 'false'

  - id: mcu_connected
    type: bool
    restore_value: no
    initial_value: 'false'

  - id: feeder_locked
    type: bool
    restore_value: yes
    initial_value: 'false'

button:
  - platform: template
    name: "Dispense Meal"
    id: dispense_meal_button
    icon: "mdi:bowl"
    on_press:
      then:
        - script.execute:
            id: feed_script
            portions: !lambda "return (int)id(portion_size).state;"

number:
  - platform: template
    name: "Portion Size"
    id: portion_size
    icon: "mdi:scale"
    entity_category: config
    min_value: 1
    max_value: 12
    step: 1
    restore_value: yes
    initial_value: 1
    optimistic: true

sensor:
  - platform: wifi_signal
    name: "RSSI"
    id: rssi_sensor
    icon: "mdi:access-point"
    entity_category: diagnostic
    update_interval: 60s
    on_value:
      then:
        - mqtt.publish:
            topic: ${topic_prefix}/rssi
            payload: !lambda "return to_string((int)x);"
            retain: true

text_sensor:
  - platform: wifi_info
    ip_address:
      name: "IP"
      id: ip_sensor
      icon: "mdi:ip-network"
      entity_category: diagnostic
      on_value:
        then:
          - mqtt.publish:
              topic: ${topic_prefix}/ip
              payload: !lambda "return x;"

script:
  # ── Tuya handshake ────────────────────────────────────────────────────────
  # Sequence confirmed from WBR3 startup capture.
  # WBR3 always sends v0 frames (55 AA 00 ...) to MCU.
  # MCU responds with v3 frames (55 AA 03 ...).
  - id: do_handshake
    mode: restart
    then:
      - logger.log: "Sending full handshake sequence"
      - uart.write: [0x55, 0xAA, 0x00, 0x00, 0x00, 0x00, 0xFF]       # heartbeat
      - delay: 200ms
      - uart.write: [0x55, 0xAA, 0x00, 0x01, 0x00, 0x00, 0x00]       # product info query
      - delay: 200ms
      - uart.write: [0x55, 0xAA, 0x00, 0x02, 0x00, 0x00, 0x01]       # MCU state query
      - delay: 200ms
      - uart.write: [0x55, 0xAA, 0x00, 0x03, 0x00, 0x01, 0x04, 0x07] # WiFi: cloud connected
      - delay: 200ms
      - uart.write: [0x55, 0xAA, 0x00, 0x08, 0x00, 0x00, 0x07]       # CMD 0x08
      - delay: 500ms
      - lambda: "id(mcu_connected) = true;"
      - mqtt.publish:
          topic: ${topic_prefix}/mcu_status
          payload: "Connected"
          retain: true
      - logger.log: "Handshake complete"

  # ── Feed script ───────────────────────────────────────────────────────────
  # Checksum verified from capture:
  #   n=1 → 0x17, n=2 → 0x18, n=4 → 0x1A  →  checksum = (0x16 + n) & 0xFF
  # Feeding done: MCU CMD 0x07 DP4=0x00, or CMD 0x34 DP11 extended report.
  - id: feed_script
    parameters:
      portions: int
    mode: single
    then:
      - mqtt.publish:
          topic: ${topic_prefix}/status
          payload: "Feeding"
          retain: true
      - lambda: |-
          int n = portions;
          if (n < 1) n = 1;
          if (n > 12) n = 12;
          uint8_t checksum = (0x16 + n) & 0xFF;
          uint8_t frame[] = {
            0x55, 0xAA, 0x00, 0x06, 0x00, 0x08,
            0x03, 0x02, 0x00, 0x04,
            0x00, 0x00, 0x00, (uint8_t)n,
            checksum
          };
          id(tuya_uart).write_array(frame, sizeof(frame));
          id(feeding_active) = true;
          ESP_LOGI("tuya", "Feed frame sent: %d portions checksum=0x%02X", n, checksum);
      # Feeding done is detected in RX poll. 30s hard timeout.
      - delay: 30s
      - if:
          condition:
            lambda: "return id(feeding_active);"
          then:
            - lambda: "id(feeding_active) = false;"
            - mqtt.publish:
                topic: ${topic_prefix}/status
                payload: "Error"
                retain: true
            - logger.log: "Feed timeout — no done signal received from MCU"

interval:
  - interval: 30s
    then:
      - mqtt.publish:
          topic: ${topic_prefix}/availability
          payload: "online"
          retain: true

  - interval: 15s
    then:
      - if:
          condition:
            lambda: "return !id(mcu_connected);"
          then:
            - logger.log: "MCU not connected — retrying handshake"
            - script.execute: do_handshake

  # ── UART RX poll (100ms) ──────────────────────────────────────────────────
  # MCU sends Tuya v3 frames (55 AA 03 ...).
  # We always reply with v0 frames (55 AA 00 ...) — MCU accepts this.
  #
  # Key frames from capture:
  #   CMD 0x00 — heartbeat (reply with 55 AA 00 00 00 00 FF)
  #   CMD 0x01 — product info (MCU sends its ID string, no reply needed)
  #   CMD 0x07 — DP status report
  #     DP4  (0x04): 0x00=idle/done, 0x02=feeding
  #     DP25 (0x19): 0x00=unlocked, 0x01=locked
  #   CMD 0x34 — extended feeding log (contains DP11 record)
  - interval: 100ms
    then:
      - lambda: |-
          static std::vector<uint8_t> buf;
          while (id(tuya_uart).available()) {
            uint8_t b;
            id(tuya_uart).read_byte(&b);
            buf.push_back(b);
          }

          while (buf.size() >= 6) {
            // Sync to 55 AA header
            if (buf[0] != 0x55 || buf[1] != 0xAA) {
              buf.erase(buf.begin());
              continue;
            }

            uint8_t  cmd         = buf[3];
            uint16_t payload_len = (buf[4] << 8) | buf[5];
            uint16_t full_len    = 6 + payload_len + 1;

            if (buf.size() < full_len) break;

            // ── CMD 0x00 Heartbeat ───────────────────────────────────────
            if (cmd == 0x00) {
              uint8_t hb[] = {0x55, 0xAA, 0x00, 0x00, 0x00, 0x00, 0xFF};
              id(tuya_uart).write_array(hb, sizeof(hb));
              if (!id(mcu_heartbeat_seen)) {
                id(mcu_heartbeat_seen) = true;
                ESP_LOGD("tuya", "First heartbeat seen");
              }
              buf.erase(buf.begin(), buf.begin() + full_len);
              continue;
            }

            // ── CMD 0x01 Product info from MCU ──────────────────────────
            if (cmd == 0x01) {
              ESP_LOGD("tuya", "CMD01 product info received (%d bytes)", payload_len);
              buf.erase(buf.begin(), buf.begin() + full_len);
              continue;
            }

            // ── CMD 0x02 MCU state report ────────────────────────────────
            if (cmd == 0x02) {
              ESP_LOGD("tuya", "CMD02 MCU state report");
              buf.erase(buf.begin(), buf.begin() + full_len);
              continue;
            }

            // ── CMD 0x07 DP status report ────────────────────────────────
            // Payload: [dp_id][dp_type][val_len_hi][val_len_lo][val...]
            if (cmd == 0x07 && payload_len >= 5 && buf.size() >= 7u + payload_len) {
              uint8_t dp_id   = buf[6];
              uint8_t dp_type = buf[7];
              uint16_t val_len = (buf[8] << 8) | buf[9];
              uint8_t dp_val  = buf[10];  // first/only value byte for bool/enum

              ESP_LOGI("tuya", "CMD07 DP%d type=0x%02X val=0x%02X", dp_id, dp_type, dp_val);

              // DP4 (0x04) = feeding motor state
              // 0x00 = idle/done, 0x01 = scheduled, 0x02 = feeding
              if (dp_id == 0x04 && dp_val == 0x00 && id(feeding_active)) {
                id(feeding_active) = false;
                auto &mqtt = *mqtt::global_mqtt_client;
                mqtt.publish("${topic_prefix}/status", "Idle", 0, true);
                ESP_LOGI("tuya", "Feeding done — DP4=0");
              }

              // DP25 (0x19) = lock state from physical button
              if (dp_id == 0x19) {
                bool locked = (dp_val == 0x01);
                id(feeder_locked) = locked;
                auto &mqtt = *mqtt::global_mqtt_client;
                mqtt.publish("${topic_prefix}/locked", locked ? "ON" : "OFF", 0, true);
                ESP_LOGI("tuya", "Lock: %s", locked ? "LOCKED" : "UNLOCKED");
              }

              buf.erase(buf.begin(), buf.begin() + full_len);
              continue;
            }

            // ── CMD 0x34 Extended feeding log ────────────────────────────
            // Sent after feed completes. Contains DP11 record with timestamp.
            // Use as fallback feeding-done signal.
            if (cmd == 0x34 && payload_len >= 2) {
              uint8_t dp_id = buf[6];
              ESP_LOGI("tuya", "CMD34 DP%d extended report", dp_id);
              if (dp_id == 0x0B && id(feeding_active)) {
                id(feeding_active) = false;
                auto &mqtt = *mqtt::global_mqtt_client;
                mqtt.publish("${topic_prefix}/status", "Idle", 0, true);
                ESP_LOGI("tuya", "Feeding done — CMD34 DP11");
              }
              buf.erase(buf.begin(), buf.begin() + full_len);
              continue;
            }

            // Unknown — log and discard
            ESP_LOGW("tuya", "Unknown CMD 0x%02X len=%d", cmd, payload_len);
            buf.erase(buf.begin(), buf.begin() + full_len);
          }
